import { ProductType } from "@/app/types";
import { Button } from "@nextui-org/react";

export default function AddToCartButton(props: ProductType){

    
    
    
    return (
        <Button className="mt-4 mb-1"> اضافه کردن به سبد</Button>
    )
}