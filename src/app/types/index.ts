export type NavbarType = {
    title: string,
    url: string,
}


export type ProductType = {
    id: number
    title: string
    description: string
    images: string[]
    price: number
    lable: string
  }